---
phase: 02-volatility-flow-agents
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tradingagents/agents/options/options_flow_analyst.py
  - tradingagents/agents/options/__init__.py
  - tradingagents/agents/__init__.py
  - tests/agents/test_options_flow_analyst.py
autonomous: true
requirements: [AGENT-02]

must_haves:
  truths:
    - "create_options_flow_analyst factory returns a callable node"
    - "Flow analyst node returns dict with options_flow_report key containing structured prose"
    - "Unusual volume detected when contract volume > 2x open interest"
    - "Put/call ratio computed from total put volume / total call volume"
    - "Net flow bias classifies as call-dominated (>60%), put-dominated (<40%), or balanced"
    - "Node does NOT write to AgentState messages"
    - "Node handles empty expirations gracefully without crashing"
  artifacts:
    - path: "tradingagents/agents/options/options_flow_analyst.py"
      provides: "Options flow analyst agent factory"
      exports: ["create_options_flow_analyst"]
      min_lines: 60
    - path: "tests/agents/test_options_flow_analyst.py"
      provides: "Unit tests for options flow analyst"
      min_lines: 50
  key_links:
    - from: "tradingagents/agents/options/options_flow_analyst.py"
      to: "tradingagents/dataflows/interface.py"
      via: "route_to_vendor calls"
      pattern: "route_to_vendor.*get_options_expirations|get_options_chain"
    - from: "tradingagents/agents/options/options_flow_analyst.py"
      to: "langchain_core.prompts"
      via: "ChatPromptTemplate.from_messages"
      pattern: "ChatPromptTemplate\\.from_messages"
    - from: "tradingagents/agents/options/__init__.py"
      to: "tradingagents/agents/options/options_flow_analyst.py"
      via: "import"
      pattern: "from .options_flow_analyst import create_options_flow_analyst"
---

<objective>
Implement the options flow analyst agent factory that detects unusual volume, computes put/call ratio, determines net flow bias from the options chain data, and uses a single LLM call to write a structured prose report.

Purpose: AGENT-02 requires a flow analyst that independently characterizes options market activity for a ticker. This completes the Phase 2 agent pair.
Output: Working `create_options_flow_analyst` factory with unit tests; options package exports updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-volatility-flow-agents/02-CONTEXT.md
@.planning/phases/02-volatility-flow-agents/02-RESEARCH.md
@.planning/phases/02-volatility-flow-agents/02-01-SUMMARY.md

@tradingagents/agents/options/volatility_analyst.py
@tradingagents/agents/options/__init__.py
@tradingagents/agents/__init__.py
@tradingagents/dataflows/interface.py

<interfaces>
<!-- Key types and contracts from Plan 01 output -->

From tradingagents/agents/options/volatility_analyst.py (Plan 01 reference for pattern):
```python
def create_volatility_analyst(llm):
    def volatility_analyst_node(state: dict) -> dict:
        # reads state["company_of_interest"], state["trade_date"]
        # fetches data via route_to_vendor(...)
        # computes metrics in Python
        # single LLM call via ChatPromptTemplate.from_messages([("system", ...), ("human", ...)])
        # returns {"volatility_report": result.content}
    return volatility_analyst_node
```

Shared helper (from Plan 01, reuse or replicate):
```python
def _parse_tabular_string(s: str) -> pd.DataFrame | None:
    """Parse whitespace-aligned tabular string from data layer."""
```

Data layer return contracts:
- `route_to_vendor("get_options_expirations", ticker)` -> list[str]
- `route_to_vendor("get_options_chain", ticker, expiration)` -> str (tabular with columns: strike, option_type, volume, open_interest, iv, delta)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement options flow analyst agent with unit tests</name>
  <files>
    tradingagents/agents/options/options_flow_analyst.py,
    tradingagents/agents/options/__init__.py,
    tradingagents/agents/__init__.py,
    tests/agents/test_options_flow_analyst.py
  </files>
  <read_first>
    tradingagents/agents/options/volatility_analyst.py,
    tradingagents/agents/options/__init__.py,
    tradingagents/agents/__init__.py,
    tradingagents/dataflows/interface.py,
    .planning/phases/02-volatility-flow-agents/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `create_options_flow_analyst(mock_llm)` returns a callable
    - Test 2: Calling the node with valid mocked data returns `{"options_flow_report": <string>}`
    - Test 3: Unusual volume detection — given chain with volume=1000, OI=400, flags as unusual (1000 > 2*400)
    - Test 4: Unusual volume non-detection — given chain with volume=500, OI=400, does NOT flag (500 < 2*400)
    - Test 5: P/C ratio — given put_vol=600, call_vol=400, ratio = 1.50
    - Test 6: P/C ratio — given call_vol=0, ratio = None (division by zero guard)
    - Test 7: Net bias — call_vol=700, put_vol=300 -> "Call-dominated (70% call volume)"
    - Test 8: Net bias — call_vol=300, put_vol=700 -> "Put-dominated (70% put volume)"
    - Test 9: Net bias — call_vol=500, put_vol=500 -> "Balanced (50% calls, 50% puts)"
    - Test 10: Empty expirations list does NOT raise IndexError; returns report acknowledging missing data
    - Test 11: Return dict does NOT contain "messages" key
  </behavior>
  <action>
    Implement `tradingagents/agents/options/options_flow_analyst.py` with the single-pass architecture per CONTEXT.md decisions:

    1. **Reuse or import `_parse_tabular_string`** from volatility_analyst.py, or duplicate the helper locally (preferred — keeps modules independent). Same implementation: `pd.read_csv(io.StringIO(s), sep=r'\s+', engine='python')`.

    2. **Helper: `_compute_flow_metrics(chain_df: pd.DataFrame) -> dict`**
       Per Research Pattern 8:
       - Split chain into calls (`option_type == "call"`) and puts (`option_type == "put"`)
       - `call_vol = calls["volume"].sum()`, `put_vol = puts["volume"].sum()`
       - `pc_ratio = put_vol / call_vol` if call_vol > 0 else None
       - Unusual volume: count contracts where `volume > 2 * open_interest` AND `open_interest > 0`
       - Identify top 3 unusual contracts by volume (strike, type, volume, OI) for reporting
       - Net flow bias: call_pct = call_vol / total_vol. >0.6 = "Call-dominated", <0.4 = "Put-dominated", else "Balanced"
       - Return dict with: `pc_ratio`, `unusual_count`, `unusual_top3` (list of dicts), `net_bias`, `total_call_vol`, `total_put_vol`

    3. **Factory: `create_options_flow_analyst(llm)`**
       Returns `options_flow_analyst_node(state)` closure:
       - Read `state["company_of_interest"]` and `state["trade_date"]`
       - Fetch: `route_to_vendor("get_options_expirations", ticker)` then `route_to_vendor("get_options_chain", ticker, expirations[0])` for nearest expiry (most active)
       - Guard: if expirations empty, build a data template saying "No options data available for {ticker}" and still invoke LLM to produce a graceful report
       - Compute metrics via `_compute_flow_metrics`
       - Build `ChatPromptTemplate.from_messages([("system", SYSTEM_PROMPT), ("human", data_template)])` — NO MessagesPlaceholder, NO bind_tools
       - Invoke: `result = (prompt | llm).invoke({})`
       - Return `{"options_flow_report": result.content}` — do NOT return "messages" key

    4. **System prompt** should instruct the LLM to write exactly one paragraph with labeled metrics:
       `"P/C Ratio: {value} ({label}). Unusual Activity: {count} contracts flagged ({details}). Net Flow: {bias}. Directional Implication: {one_line_summary}."`

    5. **Note on block/sweep detection per Research open question 3**: Do NOT claim to detect sweeps or blocks. Report unusual volume (volume > 2x OI) as "unusual volume activity". The data layer provides snapshot data, not tick data.

    6. **Note on P/C ratio history per Research open question 2**: Report the current P/C ratio only. Do NOT attempt historical comparison. Multi-day trend analysis is deferred to v2 (DATA-V2-02).

    7. **Update `tradingagents/agents/options/__init__.py`**: Add import of `create_options_flow_analyst` and add to `__all__`.

    8. **Update `tradingagents/agents/__init__.py`**: Add import of `create_options_flow_analyst` and add to `__all__`.

    Write unit tests in `tests/agents/test_options_flow_analyst.py`:
    - Mock `route_to_vendor` via `unittest.mock.patch` on the module-level import
    - Mock `llm` as MagicMock with `__or__` returning invoke-able mock
    - Test `_compute_flow_metrics` helper directly with crafted DataFrames
    - Test full node with all mocks
    - Test graceful degradation (empty expirations, empty chain)
    - Test that return dict has only "options_flow_report" key (no "messages")
  </action>
  <verify>
    <automated>python -m pytest tests/agents/test_options_flow_analyst.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def create_options_flow_analyst" tradingagents/agents/options/options_flow_analyst.py` returns 1
    - `grep -c "route_to_vendor" tradingagents/agents/options/options_flow_analyst.py` returns at least 2 (expirations, chain)
    - `grep -c "ChatPromptTemplate" tradingagents/agents/options/options_flow_analyst.py` returns at least 1
    - `grep -c "bind_tools" tradingagents/agents/options/options_flow_analyst.py` returns 0 (must NOT use bind_tools)
    - `grep -c "MessagesPlaceholder" tradingagents/agents/options/options_flow_analyst.py` returns 0 (must NOT use MessagesPlaceholder)
    - `grep "options_flow_report" tradingagents/agents/options/options_flow_analyst.py` shows `return {"options_flow_report": ...}`
    - `grep -c '"messages"' tradingagents/agents/options/options_flow_analyst.py` returns 0 in return statements
    - `grep "create_options_flow_analyst" tradingagents/agents/options/__init__.py` returns a match
    - `grep "create_options_flow_analyst" tradingagents/agents/__init__.py` returns a match
    - `python -m pytest tests/agents/test_options_flow_analyst.py -x -q` passes all tests
    - `python -m pytest tests/agents/ -x -q` passes (no regressions from Plan 01)
  </acceptance_criteria>
  <done>Options flow analyst agent factory is implemented with flow metric computations (unusual volume, P/C ratio, net bias), single-pass LLM invocation, graceful error handling, and comprehensive unit tests. All agent tests pass. Both agents exportable from tradingagents.agents.options and tradingagents.agents.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/agents/ -x -q --tb=short` — all agent tests pass (both analysts + state tests)
- `python -m pytest tests/ -x -q --tb=short` — full test suite passes (no regressions)
- `python -c "from tradingagents.agents.options import create_volatility_analyst, create_options_flow_analyst; print('imports OK')"` — both factories importable from options package
- `python -c "from tradingagents.agents import create_volatility_analyst, create_options_flow_analyst; print('top-level imports OK')"` — both factories importable from top-level agents package
</verification>

<success_criteria>
- create_options_flow_analyst factory importable from tradingagents.agents.options and tradingagents.agents
- Flow analyst computes unusual volume flags, P/C ratio, net flow bias from chain data
- Single LLM call produces structured prose report (no tool binding, no messages thread)
- All unit tests pass with mocked data layer and LLM
- Graceful handling of missing/empty data (no crashes)
- Full test suite (agents/ + dataflows/) passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-volatility-flow-agents/02-02-SUMMARY.md`
</output>
