---
phase: 05-graph-integration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/graph/test_graph_integration.py
autonomous: true
requirements: [GRAPH-01, GRAPH-03, GRAPH-04]

must_haves:
  truths:
    - "Full graph run with enable_options=True populates all 6 options state fields"
    - "Full graph run with enable_options=False produces no options-related errors"
    - "Options branch output is available to downstream debate and risk nodes"
  artifacts:
    - path: "tests/graph/test_graph_integration.py"
      provides: "End-to-end smoke tests with mocked LLMs and data layer"
      min_lines: 100
  key_links:
    - from: "tests/graph/test_graph_integration.py"
      to: "tradingagents/graph/trading_graph.py"
      via: "TradingAgentsGraph instantiation and propagate call"
      pattern: "TradingAgentsGraph|propagate"
---

<objective>
Add end-to-end integration smoke tests that invoke the full graph with mocked LLMs and data layer, verifying options-enabled and equity-only modes produce correct state.

Purpose: Validates that the graph wiring from Plan 01 actually works end-to-end, not just structurally.
Output: Extended test_graph_integration.py with smoke tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-graph-integration/05-CONTEXT.md
@.planning/phases/05-graph-integration/05-RESEARCH.md
@.planning/phases/05-graph-integration/05-01-SUMMARY.md

@tradingagents/graph/setup.py
@tradingagents/graph/trading_graph.py
@tradingagents/graph/propagation.py
@tradingagents/agents/options/__init__.py
@tradingagents/default_config.py
@tests/graph/test_graph_integration.py
@tests/agents/test_volatility_analyst.py

<interfaces>
<!-- From Plan 01 output: setup.py now has options branch -->

From tradingagents/graph/setup.py (after Plan 01):
```python
OPTIONS_NODES = [
    ("Options: Volatility Analyst",    create_volatility_analyst),
    ("Options: Flow Analyst",          create_options_flow_analyst),
    ("Options: Strategy Selector",     create_options_strategy_selector),
    ("Options: Strike/Expiry",         create_strike_expiry_selector),
    ("Options: Pricing Agent",         create_options_pricing_agent),
    ("Options: Legs Builder",          create_options_legs_builder),
    ("Options: Greeks Monitor",        create_greeks_monitor),
]
```

Each options agent factory returns `(state: dict) -> dict` with one key:
- create_volatility_analyst -> {"volatility_report": "..."}
- create_options_flow_analyst -> {"options_flow_report": "..."}
- create_options_strategy_selector -> {"options_strategy": "..."}
- create_strike_expiry_selector -> {"options_legs": "..."}
- create_options_pricing_agent -> {"options_pricing_report": "..."}
- create_options_legs_builder -> {"options_legs": "..."}
- create_greeks_monitor -> {"greeks_report": "..."}

From tradingagents/graph/trading_graph.py:
```python
class TradingAgentsGraph:
    def __init__(self, selected_analysts=[...], debug=False, config=None, callbacks=None):
    def propagate(self, company_name, trade_date):
        # Returns (final_state, processed_signal)
```

From tradingagents/default_config.py:
```python
DEFAULT_CONFIG = {
    "enable_options": False,
    "options_vendor": "tradier",
    "options_delta_target": 0.30,
    "options_dte_window": [21, 45],
    "options_min_oi": 100,
    # ... other keys ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add end-to-end integration smoke tests</name>
  <files>tests/graph/test_graph_integration.py</files>
  <read_first>
    - tests/graph/test_graph_integration.py
    - tradingagents/graph/trading_graph.py
    - tradingagents/graph/setup.py
    - tradingagents/graph/propagation.py
    - tradingagents/default_config.py
    - tradingagents/dataflows/config.py
    - tests/agents/test_volatility_analyst.py
    - tests/agents/test_options_pricing_agent.py
  </read_first>
  <behavior>
    - test_full_graph_options_enabled_populates_all_fields: With enable_options=True and all LLMs/data mocked, graph.invoke produces final_state where all 6 options keys are non-empty strings
    - test_full_graph_equity_only_no_options_content: With enable_options=False and all LLMs/data mocked, graph.invoke produces final_state where options fields are empty strings and equity fields are populated
    - test_options_branch_failure_graceful_degradation: With enable_options=True and one options agent raising Exception, graph still completes and failed agent's field is empty string while others succeed
  </behavior>
  <action>
    Extend `tests/graph/test_graph_integration.py` with 3 new end-to-end smoke tests.

    **Mocking strategy:** Mock at the highest practical level to avoid deep dependency chains:

    1. Mock all LLM calls: Patch `tradingagents.llm_clients.create_llm_client` to return a MagicMock whose `.get_llm()` returns a mock LLM. The mock LLM must handle both `.invoke()` (returns MagicMock with `.content = "mock report"` and `.tool_calls = []`) and direct call `__call__` (same return).

    2. Mock all data layer calls: Patch `tradingagents.dataflows.interface.route_to_vendor` to return `""` (empty string — options agents handle empty data gracefully). Also patch `tradingagents.agents.utils.agent_utils.get_stock_data` and all other tool functions to return simple string results.

    3. Mock memory: Patch `tradingagents.agents.utils.memory.FinancialSituationMemory` to return a MagicMock.

    **Test: `test_full_graph_options_enabled_populates_all_fields`**
    - Create config dict: `{**DEFAULT_CONFIG, "enable_options": True}`
    - Instantiate `TradingAgentsGraph(selected_analysts=["market"], config=config)` (use only 1 analyst to minimize graph size)
    - Call `graph.propagate("AAPL", "2025-01-15")`
    - Assert `final_state["volatility_report"] != ""`
    - Assert `final_state["options_flow_report"] != ""`
    - Assert `final_state["options_strategy"] != ""`
    - Assert `final_state["options_legs"] != ""`
    - Assert `final_state["options_pricing_report"] != ""`
    - Assert `final_state["greeks_report"] != ""`
    - Assert `final_state["market_report"] != ""` (equity still works)

    **Test: `test_full_graph_equity_only_no_options_content`**
    - Create config dict: `{**DEFAULT_CONFIG, "enable_options": False}`
    - Instantiate and propagate same as above
    - Assert `final_state["volatility_report"] == ""`
    - Assert `final_state["market_report"] != ""` (equity works)
    - Assert no errors raised

    **Test: `test_options_branch_failure_graceful_degradation`**
    - Create config with `enable_options=True`
    - Patch `create_options_flow_analyst` to return a function that raises `RuntimeError("data unavailable")`
    - Propagate and assert `final_state["options_flow_report"] == ""` (graceful fallback)
    - Assert `final_state["volatility_report"] != ""` (upstream succeeded)
    - Note: downstream options agents after the failed one may also be empty since they depend on prior state. This is acceptable per the graceful degradation decision — the key assertion is that the graph completes without error and equity output is unaffected.

    If the full `TradingAgentsGraph` is too complex to instantiate with mocks (due to tool node creation, etc.), use an alternative approach: directly instantiate `GraphSetup` with mocked components, call `setup_graph()`, then `graph.invoke(initial_state)` where `initial_state` comes from `Propagator().create_initial_state("AAPL", "2025-01-15")`. This avoids the `TradingAgentsGraph.__init__` complexity while still testing the actual graph execution.
  </action>
  <verify>
    <automated>cd "C:\Users\maker\Documents\Projects\agents-for-trades" && python -m pytest tests/graph/test_graph_integration.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/graph/test_graph_integration.py contains `def test_full_graph_options_enabled_populates_all_fields`
    - tests/graph/test_graph_integration.py contains `def test_full_graph_equity_only_no_options_content`
    - tests/graph/test_graph_integration.py contains `def test_options_branch_failure_graceful_degradation`
    - tests/graph/test_graph_integration.py contains `"enable_options": True`
    - tests/graph/test_graph_integration.py contains `"enable_options": False`
    - tests/graph/test_graph_integration.py contains `assert final_state` or equivalent assertion on state fields
    - `python -m pytest tests/graph/test_graph_integration.py -x -q` exits 0
    - `python -m pytest tests/ -q --tb=short` exits 0 (full suite, no regressions)
  </acceptance_criteria>
  <done>Integration smoke tests pass. Options-enabled run populates all 6 fields. Equity-only run leaves options fields empty. Graceful degradation confirmed. Full test suite green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/graph/ -x -q` — all graph tests pass
- `python -m pytest tests/ -q --tb=short` — full suite green, no regressions
- Options-enabled smoke test confirms all 6 state fields are non-empty
- Equity-only smoke test confirms options fields are empty and no errors
- Graceful degradation test confirms graph completes despite agent failure
</verification>

<success_criteria>
- End-to-end graph run with options enabled produces 6 non-empty options state fields (GRAPH-01, GRAPH-03)
- End-to-end graph run with options disabled produces identical structure to pre-extension baseline (GRAPH-04)
- Graceful degradation verified: agent failure does not crash graph
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-graph-integration/05-02-SUMMARY.md`
</output>
