---
phase: 07-visual-frontend
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-04"]
files_modified: []
autonomous: false
requirements:
  - FE-VERIFY-01
must_haves:
  truths:
    - "The full frontend renders in a browser at localhost:5173"
    - "The sidebar contains all configuration fields"
    - "The tab bar shows equity report tabs and hides options tabs when toggle is off"
    - "The backend API starts and responds to requests"
---

<objective>
Visual verification checkpoint: confirm that the frontend renders correctly in a browser and the backend API starts without errors. This is a human-verify checkpoint that does not modify any files.

Purpose: Catch visual/layout issues and API startup problems before considering the phase complete.

Output: Human confirmation that the UI looks correct and the backend starts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-visual-frontend/07-01-SUMMARY.md
@.planning/phases/07-visual-frontend/07-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of frontend and backend</name>
  <files></files>
  <action>
This is a human verification checkpoint. No code changes needed.

Start both servers and verify the UI renders correctly:

1. Start the backend:
   cd C:\Users\maker\Documents\Projects\agents-for-trades
   uvicorn api.main:app --reload --port 8000
   Verify: no startup errors, terminal shows "Uvicorn running on http://127.0.0.1:8000"

2. Start the frontend (separate terminal):
   cd C:\Users\maker\Documents\Projects\agents-for-trades\frontend
   npm run dev
   Verify: terminal shows "Local: http://localhost:5173/"

3. Open http://localhost:5173 in browser. Verify:
   - Left sidebar visible with "TradingAgents" heading
   - Ticker input field present
   - Date picker present
   - 5 analyst checkboxes (Market, Technical, Social Media, News, Fundamentals)
   - "Enable Options Analysis" toggle
   - LLM Provider dropdown
   - Deep Think Model and Quick Think Model text inputs
   - "Analyze" button (disabled when ticker is empty)

4. Type "AAPL" in ticker field. Verify Analyze button becomes enabled.

5. Toggle "Enable Options Analysis" on. Verify additional options tabs appear.

6. Toggle it off. Verify options tabs disappear.
  </action>
  <verify>Human confirms visual verification passes. Type "approved" or describe issues.</verify>
  <done>Backend starts on port 8000, frontend renders on port 5173, all sidebar fields present, options tabs toggle correctly, layout matches sidebar + main area design.</done>
</task>

</tasks>

<verification>
Human confirms visual verification passes.
</verification>

<success_criteria>
- Backend starts without errors on port 8000
- Frontend renders in browser on port 5173
- All sidebar fields present and functional
- Tab bar shows/hides options tabs based on toggle
- Layout matches: sidebar (left) + main area (right) with progress stepper (top) + tabs + content
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-frontend/07-05-SUMMARY.md`
</output>
