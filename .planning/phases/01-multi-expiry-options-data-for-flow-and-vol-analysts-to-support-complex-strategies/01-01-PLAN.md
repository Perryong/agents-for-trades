---
phase: 01-multi-expiry-options-data-for-flow-and-vol-analysts-to-support-complex-strategies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/agents/options/constants.py
  - tradingagents/agents/options/strike_expiry_selector.py
  - tests/agents/test_constants.py
autonomous: true
requirements: [MEX-01]

must_haves:
  truths:
    - "DTE_BUCKETS is importable from tradingagents.agents.options.constants"
    - "strike_expiry_selector uses the shared DTE_BUCKETS constant, not a local copy"
    - "All existing strike_expiry_selector tests still pass"
  artifacts:
    - path: "tradingagents/agents/options/constants.py"
      provides: "Shared DTE_BUCKETS constant"
      contains: "DTE_BUCKETS"
    - path: "tests/agents/test_constants.py"
      provides: "Validation that DTE_BUCKETS has 4 entries with correct structure"
  key_links:
    - from: "tradingagents/agents/options/strike_expiry_selector.py"
      to: "tradingagents/agents/options/constants.py"
      via: "from tradingagents.agents.options.constants import DTE_BUCKETS"
      pattern: "from tradingagents\\.agents\\.options\\.constants import DTE_BUCKETS"
---

<objective>
Extract the DTE_BUCKETS constant from strike_expiry_selector into a shared constants module so all three options agents (strike_expiry_selector, options_flow_analyst, volatility_analyst) use the same bucket definitions.

Purpose: Establish the shared constant that Plans 02 and 03 depend on for multi-bucket chain fetching.
Output: `constants.py` with DTE_BUCKETS, `strike_expiry_selector.py` updated to import it, tests for the constant.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-expiry-options-data-for-flow-and-vol-analysts-to-support-complex-strategies/01-CONTEXT.md
@.planning/phases/01-multi-expiry-options-data-for-flow-and-vol-analysts-to-support-complex-strategies/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From tradingagents/agents/options/strike_expiry_selector.py (lines 265-270) — the authoritative source to extract:
```python
DTE_BUCKETS = [
    {"label": "Short-term (0-5 DTE)", "min": 0, "max": 5, "tag": "SHORT"},
    {"label": "Weekly (5-14 DTE)", "min": 5, "max": 14, "tag": "WEEKLY"},
    {"label": "Monthly (14-45 DTE)", "min": 14, "max": 45, "tag": "MONTHLY"},
    {"label": "Longer-term (45-90 DTE)", "min": 45, "max": 90, "tag": "LONGER"},
]
```

This is currently defined INSIDE the `create_strike_expiry_selector` factory function as a local variable (line 265). It must be moved to a module-level constant in `constants.py`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create constants.py and test_constants.py, then update strike_expiry_selector</name>
  <files>
    tradingagents/agents/options/constants.py,
    tradingagents/agents/options/strike_expiry_selector.py,
    tests/agents/test_constants.py
  </files>
  <read_first>
    tradingagents/agents/options/strike_expiry_selector.py (lines 260-270 for DTE_BUCKETS definition; lines 340-408 for usage in bucket loop),
    tests/agents/test_constants.py (does not exist yet — will create)
  </read_first>
  <behavior>
    - Test 1: `from tradingagents.agents.options.constants import DTE_BUCKETS` succeeds without ImportError
    - Test 2: `len(DTE_BUCKETS) == 4` — exactly 4 buckets
    - Test 3: Each bucket dict has keys: "label", "min", "max", "tag"
    - Test 4: Tags are exactly ["SHORT", "WEEKLY", "MONTHLY", "LONGER"] in order
    - Test 5: Bucket ranges are (0,5), (5,14), (14,45), (45,90) — matching the authoritative source
  </behavior>
  <action>
    1. Create `tradingagents/agents/options/constants.py` with a single module-level constant:
       ```python
       """Shared constants for options agents."""

       DTE_BUCKETS = [
           {"label": "Short-term (0-5 DTE)", "min": 0, "max": 5, "tag": "SHORT"},
           {"label": "Weekly (5-14 DTE)", "min": 5, "max": 14, "tag": "WEEKLY"},
           {"label": "Monthly (14-45 DTE)", "min": 14, "max": 45, "tag": "MONTHLY"},
           {"label": "Longer-term (45-90 DTE)", "min": 45, "max": 90, "tag": "LONGER"},
       ]
       ```

    2. Create `tests/agents/test_constants.py` with the 5 tests described in behavior section.

    3. In `tradingagents/agents/options/strike_expiry_selector.py`:
       - Add import at the top of the file (after other imports): `from tradingagents.agents.options.constants import DTE_BUCKETS`
       - Remove the local `DTE_BUCKETS = [...]` definition at line 265-270 inside `create_strike_expiry_selector`
       - The bucket loop at lines 340+ already references `DTE_BUCKETS` by name, so no other changes needed inside the function body

    IMPORTANT: Do NOT change any other logic in strike_expiry_selector.py. The bucket selection loop, `_build_legs_for_expiry`, `_select_contract`, and all other functions must remain untouched.
  </action>
  <verify>
    <automated>pytest tests/agents/test_constants.py -x -q && pytest tests/ -x -q --ignore=tests/graph</automated>
  </verify>
  <acceptance_criteria>
    - `tradingagents/agents/options/constants.py` exists and contains `DTE_BUCKETS = [`
    - `tests/agents/test_constants.py` exists and contains `def test_` (at least 4 test functions)
    - `tradingagents/agents/options/strike_expiry_selector.py` contains `from tradingagents.agents.options.constants import DTE_BUCKETS`
    - `tradingagents/agents/options/strike_expiry_selector.py` does NOT contain a local `DTE_BUCKETS = [` inside the factory function body
    - `pytest tests/agents/test_constants.py -x -q` exits 0
    - `pytest tests/ -x -q --ignore=tests/graph` exits 0 (all existing tests pass)
  </acceptance_criteria>
  <done>DTE_BUCKETS is a shared constant importable by all agents, strike_expiry_selector uses the shared import, all tests green.</done>
</task>

</tasks>

<verification>
- `python -c "from tradingagents.agents.options.constants import DTE_BUCKETS; assert len(DTE_BUCKETS) == 4"`
- `pytest tests/agents/test_constants.py -x -q` — all 5 tests pass
- `pytest tests/ -x -q --ignore=tests/graph` — full suite green (no regressions)
</verification>

<success_criteria>
- constants.py exists with DTE_BUCKETS (4 entries matching authoritative source)
- strike_expiry_selector imports from constants (no local definition)
- All existing tests pass unchanged
- New test_constants.py validates structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-expiry-options-data-for-flow-and-vol-analysts-to-support-complex-strategies/01-01-SUMMARY.md`
</output>
