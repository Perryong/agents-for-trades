---
phase: 01-trade-recommendation-sidebar-lock-in-flow
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/api/test_trade_routes.py
  - tests/api/test_chart_routes.py
  - tests/api/test_dashboard_routes.py
  - tests/api/test_price_routes.py
autonomous: true
requirements:
  - D-03
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-15
  - D-16
  - D-17
  - D-18

must_haves:
  truths:
    - "10 test stubs exist across 4 test files, all initially failing or skipped"
    - "Test files import from api modules without error"
    - "pytest discovers all 10 tests"
  artifacts:
    - path: "tests/api/test_price_routes.py"
      provides: "Test stub for D-03 live price endpoint"
      contains: "test_live_price"
    - path: "tests/api/test_trade_routes.py"
      provides: "Test stubs for D-09, D-10, D-11, D-12, D-15, D-18"
      contains: "test_bracket_submit"
    - path: "tests/api/test_chart_routes.py"
      provides: "Test stub for D-13 structured JSON overlay"
      contains: "test_structured_json_overlay"
    - path: "tests/api/test_dashboard_routes.py"
      provides: "Test stubs for D-16, D-17"
      contains: "test_risk_reward"
  key_links: []
---

<objective>
Create Wave 0 test infrastructure: 10 test stubs across 4 files covering all testable phase requirements.

Purpose: Nyquist compliance requires test stubs to exist before implementation begins. These stubs define the expected behavior contract for Plans 01-01 through 01-05. Each stub should fail or be marked with `pytest.mark.skip(reason="Wave 0 stub — implementation in Plan XX")` until the corresponding plan implements the feature.
Output: 4 test files with 10 total test stubs, all discoverable by pytest.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-trade-recommendation-sidebar-lock-in-flow/01-CONTEXT.md
@.planning/phases/01-trade-recommendation-sidebar-lock-in-flow/01-RESEARCH.md
@.planning/phases/01-trade-recommendation-sidebar-lock-in-flow/01-VALIDATION.md

<interfaces>
From tests/api/test_trade_routes.py (existing test patterns):
```python
import pytest
from unittest.mock import AsyncMock, patch, MagicMock
from api.schemas import TradeRequest
# Tests use httpx AsyncClient with app fixture
```

From tests/api/test_chart_routes.py (existing test patterns):
```python
import pytest
from unittest.mock import AsyncMock, patch, MagicMock
# Tests use direct function calls or httpx AsyncClient
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for trade routes (6 tests) and price routes (1 test)</name>
  <read_first>
    - tests/api/test_trade_routes.py
    - tests/api/test_price_routes.py (may not exist)
    - api/trade_routes.py
    - api/schemas.py
  </read_first>
  <files>tests/api/test_trade_routes.py, tests/api/test_price_routes.py</files>
  <action>
**tests/api/test_trade_routes.py** — Append 6 new test stubs to the existing file. Do NOT modify existing tests. Add after the last existing test function:

```python
# === Wave 0 stubs for Phase 1 ===

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-03")
@pytest.mark.asyncio
async def test_bracket_submit():
    """D-09: POST /api/trades/bracket submits Alpaca bracket order with TP + SL legs."""
    # Expects: 200 response with order_id, bracket_tp_order_id and bracket_sl_order_id stored on Trade
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-03")
@pytest.mark.asyncio
async def test_no_autoclose_endpoint():
    """D-10: check-autoclose endpoint is removed entirely."""
    # Expects: /trades/check-autoclose route does NOT exist in trade_router.routes
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-03")
@pytest.mark.asyncio
async def test_bracket_tif():
    """D-11: TIF field (GTC or DAY) passed through to Alpaca bracket order."""
    # Expects: MarketOrderRequest or LimitOrderRequest receives time_in_force matching request.tif
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-03")
@pytest.mark.asyncio
async def test_expired_entry_no_outcome():
    """D-12: Expired entry order gets close_reason='Expired' with no outcome or pnl_pct."""
    # Expects: trade.status='expired', trade.close_reason='Expired', trade.outcome=None, trade.pnl_pct=None
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-03")
@pytest.mark.asyncio
async def test_close_reason_target_hit():
    """D-15: When TP bracket leg fills, close_reason is set to 'Target Hit'."""
    # Expects: trade.close_reason='Target Hit', trade.status='closed', trade.close_price set
    pass

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-05")
@pytest.mark.asyncio
async def test_legacy_delete():
    """D-18: DELETE /api/trades/legacy removes trades with no bracket leg IDs."""
    # Expects: trades without bracket_tp_order_id/bracket_sl_order_id and not active are deleted
    pass
```

**tests/api/test_price_routes.py** — Create new file with 1 test stub:

```python
"""Tests for live price endpoint (D-03)."""
import pytest


@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-01")
@pytest.mark.asyncio
async def test_live_price():
    """D-03: GET /api/price/{ticker}/live returns price + change_pct from Alpaca data client."""
    # Expects: 200 response with ticker, price, open, change_pct, timestamp fields
    # Expects: change_pct = (close - open) / open * 100
    pass
```
  </action>
  <verify>
    <automated>cd "C:/Users/maker/Documents/Projects/agents-for-trades" && python -m pytest tests/api/test_trade_routes.py tests/api/test_price_routes.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>7 test stubs exist in test_trade_routes.py (6 new) and test_price_routes.py (1 new), all discoverable by pytest</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for chart routes (1 test) and dashboard routes (2 tests)</name>
  <read_first>
    - tests/api/test_chart_routes.py
    - tests/api/test_score_routes.py (dashboard tests may be here)
    - api/chart_routes.py
    - api/dashboard_routes.py
  </read_first>
  <files>tests/api/test_chart_routes.py, tests/api/test_dashboard_routes.py</files>
  <action>
**tests/api/test_chart_routes.py** — Append 1 new test stub to the existing file. Do NOT modify existing tests:

```python
# === Wave 0 stub for Phase 1 ===

@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-01")
def test_structured_json_overlay():
    """D-13: Structured JSON block parsed from trader output, with regex fallback."""
    # Expects: _parse_structured_json('text ```json {"signal":"BUY","entry_price":100.0} ```') returns dict
    # Expects: _parse_structured_json('no json here') returns None
    # Expects: get_chart_overlay tries JSON first, falls back to regex
    pass
```

**tests/api/test_dashboard_routes.py** — Create new file with 2 test stubs:

```python
"""Tests for dashboard extensions (D-16, D-17)."""
import pytest


@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-05")
@pytest.mark.asyncio
async def test_risk_reward():
    """D-16: Dashboard summary includes avg_risk_reward ratio."""
    # Expects: For BUY trade with fill=100, target=120, stop=90:
    #   risk = 100 - 90 = 10, reward = 120 - 100 = 20, ratio = 2.0
    # Expects: avg_risk_reward field present in DashboardSummaryResponse
    pass


@pytest.mark.skip(reason="Wave 0 stub — implementation in Plan 01-05")
@pytest.mark.asyncio
async def test_r_multiple():
    """D-17: Dashboard summary includes avg_r_multiple."""
    # Expects: For BUY trade with fill=100, stop=90, pnl_pct=15.0:
    #   risk_pct = |100 - 90| / 100 * 100 = 10.0
    #   r_multiple = 15.0 / 10.0 = 1.5
    # Expects: avg_r_multiple field present in DashboardSummaryResponse
    pass
```
  </action>
  <verify>
    <automated>cd "C:/Users/maker/Documents/Projects/agents-for-trades" && python -m pytest tests/api/test_chart_routes.py tests/api/test_dashboard_routes.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>3 test stubs exist in test_chart_routes.py (1 new) and test_dashboard_routes.py (2 new), all discoverable by pytest</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/api/test_trade_routes.py tests/api/test_price_routes.py tests/api/test_chart_routes.py tests/api/test_dashboard_routes.py --collect-only -q` discovers all 10 new stubs
- `python -m pytest tests/api/ -x -q` passes (stubs are skipped, existing tests unaffected)
</verification>

<success_criteria>
- 10 test stubs across 4 files: test_trade_routes.py (6), test_price_routes.py (1), test_chart_routes.py (1), test_dashboard_routes.py (2)
- All stubs are pytest.mark.skip with Wave 0 reason
- Existing tests in test_trade_routes.py and test_chart_routes.py are NOT modified
- pytest --collect-only discovers all 10 stubs
- pytest full suite still passes (skipped stubs don't break anything)
</success_criteria>

<output>
After completion, create `.planning/phases/01-trade-recommendation-sidebar-lock-in-flow/01-00-SUMMARY.md`
</output>
